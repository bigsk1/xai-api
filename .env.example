# Required API keys
XAI_API_KEY=your_xai_api_key_here

# Optional: API Base URL (default: https://api.x.ai/v1)
XAI_API_BASE=https://api.x.ai/v1

# Optional: Default models
DEFAULT_CHAT_MODEL=grok-3-mini-beta
DEFAULT_IMAGE_GEN_MODEL=grok-2-image
DEFAULT_VISION_MODEL=grok-2-vision-latest

# Optional: Rate limiting
API_RATE_LIMIT=100
API_RATE_LIMIT_PERIOD=3600

# Note: The xAI API doesn't currently support quality, size, and style parameters for image generation 